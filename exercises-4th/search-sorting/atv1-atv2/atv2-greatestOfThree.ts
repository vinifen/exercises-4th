// GREATER THAN THREE – In the same class as the previous exercise, implement a method that finds the
// largest of the three values. Two conditional structures (all without the otherwise block) are
// sufficient. Do not use a predefined method to return the largest value.

import Order from "./Order";

const order = new Order(1, 5, 1);
console.log(order.greatestOfThree());